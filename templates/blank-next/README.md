# OpenForge JavaScript starter

This is a normal standalone Next.js App Router project. OpenForge is not a
runtime dependency.

```bash
pnpm install
pnpm dev
```

The starter demonstrates JavaScript/JSX, server components, Tailwind CSS,
CSS Modules, portable CSS design tokens, metadata, and accessible focus styles.
